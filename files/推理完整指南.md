# LISA 推理完整指南

## 📋 目录
1. [推理所需预训练模型](#推理所需预训练模型)
2. [环境安装](#环境安装)
3. [模型下载](#模型下载)
4. [推理操作步骤](#推理操作步骤)
5. [数据集需求](#数据集需求)
6. [常见问题](#常见问题)

---

## 🔍 推理所需预训练模型

### 必需模型（必须下载）

#### 1. LISA 主模型（二选一）

**选项 A：13B 模型（推荐，性能更好）**
- **标准版**：`xinlai/LISA-13B-llama2-v1`
  - HuggingFace 链接：https://huggingface.co/xinlai/LISA-13B-llama2-v1
  - 模型大小：约 26 GB
  - 显存需求：30GB（fp16/bf16）或 16GB（8-bit）或 9GB（4-bit）

- **解释版**：`xinlai/LISA-13B-llama2-v1-explanatory`
  - HuggingFace 链接：https://huggingface.co/xinlai/LISA-13B-llama2-v1-explanatory
  - 模型大小：约 26 GB
  - 特点：可以输出解释性文本

**选项 B：7B 模型（显存较小）**
- **标准版**：`xinlai/LISA-7B-v1`
  - HuggingFace 链接：https://huggingface.co/xinlai/LISA-7B-v1
  - 模型大小：约 14 GB
  - 显存需求：15GB（fp16）或 6GB（4-bit）

- **解释版**：`xinlai/LISA-7B-v1-explanatory`
  - HuggingFace 链接：https://huggingface.co/xinlai/LISA-7B-v1-explanatory
  - 模型大小：约 14 GB

### 自动下载的模型（无需手动下载）

#### 2. CLIP 视觉编码器
- **模型名称**：`openai/clip-vit-large-patch14`
- **下载方式**：首次运行时自动从 HuggingFace 下载
- **大小**：约 800 MB
- **用途**：处理输入图像，提取视觉特征

#### 3. SAM 组件（内置）
- SAM（Segment Anything Model）的组件已集成在 LISA 模型中
- 无需单独下载 SAM 权重
- 用于生成分割 mask

---

## 🛠️ 环境安装

### 1. Python 环境

```bash
# 推荐使用 Python 3.8-3.10
python --version  # 检查版本
```

### 2. 安装依赖

```bash
cd /home/liying/Desktop/IMAGE_EDITE-CVPR-2025/LISA

# 安装基础依赖
pip install -r requirements.txt

# 安装 flash-attention（可选，用于加速）
pip install flash-attn --no-build-isolation
```

### 3. 关键依赖说明

根据 `requirements.txt`，主要依赖包括：
- `torch==1.13.1`（需要 CUDA 11.7 支持）
- `transformers==4.31.0`
- `bitsandbytes==0.41.1`（用于量化）
- `gradio==3.39.0`（用于 Web 界面）
- `peft==0.4.0`（用于 LoRA）

### 4. 验证安装

```bash
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
python -c "import transformers; print(transformers.__version__)"
```

---

## 📥 模型下载

### 方法 1：自动下载（推荐）

模型会在首次运行时自动从 HuggingFace 下载：

```bash
# 运行时会自动下载
python chat.py --version='xinlai/LISA-13B-llama2-v1'
```

**下载位置**：
- 默认：`~/.cache/huggingface/hub/`
- 可以通过设置环境变量 `HF_HOME` 或 `TRANSFORMERS_CACHE` 自定义

### 方法 2：手动下载

如果需要手动下载或离线使用：

```bash
# 使用 huggingface-cli
pip install huggingface_hub
huggingface-cli download xinlai/LISA-13B-llama2-v1 --local-dir ./models/LISA-13B-llama2-v1

# 或使用 Python
from huggingface_hub import snapshot_download
snapshot_download(
    repo_id="xinlai/LISA-13B-llama2-v1",
    local_dir="./models/LISA-13B-llama2-v1"
)
```

### 方法 3：使用本地模型路径

如果已经下载到本地：

```bash
python chat.py --version='./models/LISA-13B-llama2-v1'
```

---

## 🚀 推理操作步骤

### 方式 1：命令行推理（chat.py）

#### 基础命令

```bash
cd /home/liying/Desktop/IMAGE_EDITE-CVPR-2025/LISA

# 13B 模型，16-bit 精度（需要 30GB 显存）
CUDA_VISIBLE_DEVICES=0 python chat.py --version='xinlai/LISA-13B-llama2-v1'

# 使用 bf16 精度（A100/H100 推荐）
CUDA_VISIBLE_DEVICES=0 python chat.py --version='xinlai/LISA-13B-llama2-v1' --precision='bf16'

# 使用 fp16 精度
CUDA_VISIBLE_DEVICES=0 python chat.py --version='xinlai/LISA-13B-llama2-v1' --precision='fp16'
```

#### 量化推理（显存不足时）

```bash
# 8-bit 量化（需要 16GB 显存）
CUDA_VISIBLE_DEVICES=0 python chat.py \
  --version='xinlai/LISA-13B-llama2-v1' \
  --precision='fp16' \
  --load_in_8bit

# 4-bit 量化（需要 9GB 显存，推荐）
CUDA_VISIBLE_DEVICES=0 python chat.py \
  --version='xinlai/LISA-13B-llama2-v1' \
  --precision='fp16' \
  --load_in_4bit
```

#### 使用 7B 模型（显存更小）

```bash
# 7B 模型，fp16 精度（需要 15GB 显存）
CUDA_VISIBLE_DEVICES=0 python chat.py --version='xinlai/LISA-7B-v1' --precision='fp16'

# 7B 模型，4-bit 量化（需要 6GB 显存）
CUDA_VISIBLE_DEVICES=0 python chat.py \
  --version='xinlai/LISA-7B-v1' \
  --precision='fp16' \
  --load_in_4bit
```

#### 使用解释版模型

```bash
# 13B 解释版
CUDA_VISIBLE_DEVICES=0 python chat.py \
  --version='xinlai/LISA-13B-llama2-v1-explanatory' \
  --precision='fp16' \
  --load_in_4bit
```

#### 交互式使用

运行后，按提示输入：

```
Please input your prompt: Where can the driver see the car speed in this image? Please output segmentation mask.
Please input the image path: imgs/example1.jpg
```

**输出结果**：
- 分割 mask 图像：`vis_output/example1_mask_0.jpg`
- 可视化结果：`vis_output/example1_masked_img_0.jpg`
- 文本输出：控制台显示

### 方式 2：Web 界面推理（app.py）

#### 启动 Web 服务

```bash
cd /home/liying/Desktop/IMAGE_EDITE-CVPR-2025/LISA

# 默认使用 4-bit 量化
CUDA_VISIBLE_DEVICES=0 python app.py \
  --version='xinlai/LISA-13B-llama2-v1' \
  --load_in_4bit

# 8-bit 推理
CUDA_VISIBLE_DEVICES=0 python app.py \
  --version='xinlai/LISA-13B-llama2-v1' \
  --load_in_8bit

# 16-bit 推理（不使用量化）
CUDA_VISIBLE_DEVICES=0 python app.py \
  --version='xinlai/LISA-13B-llama2-v1'
```

#### 访问界面

- 默认地址：`http://localhost:7860`
- 如果服务器有公网 IP，可以设置：`demo.launch(server_name="0.0.0.0", server_port=8080)`

#### 使用界面

1. 在 "Text Instruction" 输入框中输入提示词
2. 在 "Input Image" 上传或选择图片
3. 点击提交，等待结果
4. 查看分割结果和文本输出

#### 提示词格式建议

**标准分割**：
```
Can you segment xxx in this image?
What is xxx in this image? Please output segmentation mask.
```

**带解释的分割**：
```
What is xxx in this image? Please output segmentation mask and explain why.
Where can the driver see the car speed in this image? Please output segmentation mask and explain.
```

---

## 📊 数据集需求

### ⚠️ 重要说明：推理不需要数据集！

LISA 推理是**零样本（zero-shot）**的，只需要：
1. **输入图片**：任意图片（JPG、PNG 等格式）
2. **文本提示**：描述你想要分割的内容

### 示例图片

你可以使用任何图片进行测试：
- 自己的照片
- 网络图片
- 项目中的示例图片（`imgs/` 目录）

### 测试图片准备

```bash
# 创建测试目录
mkdir -p test_images

# 将测试图片放入 test_images/
# 例如：test_images/dog.jpg, test_images/car.jpg
```

### 提示词示例

| 场景 | 提示词示例 |
|------|-----------|
| 物体分割 | "Can you segment the dog in this image?" |
| 功能分割 | "Where can the driver see the car speed in this image? Please output segmentation mask." |
| 推理分割 | "What can make the woman stand higher? Please output segmentation mask and explain why." |
| 复杂查询 | "Can you segment the unusual part in this image and explain why." |

---

## 💻 显存需求总结

| 模型 | 精度 | VRAM 需求 | 推荐 GPU |
|------|------|-----------|----------|
| 13B | fp32/bf16/fp16 | ~30 GB | A100 40GB |
| 13B | 8-bit | ~16 GB | RTX 3090/4090 24GB |
| 13B | 4-bit | ~9 GB | RTX 3060/3070 12GB |
| 7B | fp16 | ~15 GB | RTX 3090/4090 24GB |
| 7B | 4-bit | ~6 GB | RTX 3060 12GB |

### 显存优化建议

1. **使用量化**：4-bit 量化可以大幅降低显存需求
2. **使用 7B 模型**：如果 13B 显存不足，使用 7B 模型
3. **降低图像分辨率**：修改 `--image_size` 参数（默认 1024）

---

## 🔧 常见问题

### 1. 模型下载失败

**问题**：无法从 HuggingFace 下载模型

**解决方案**：
```bash
# 设置 HuggingFace token（如果需要）
export HF_TOKEN=your_token_here

# 或使用镜像站点
export HF_ENDPOINT=https://hf-mirror.com

# 手动下载
huggingface-cli download xinlai/LISA-13B-llama2-v1
```

### 2. 显存不足（OOM）

**问题**：`RuntimeError: CUDA out of memory`

**解决方案**：
```bash
# 使用 4-bit 量化
--load_in_4bit

# 或使用 7B 模型
--version='xinlai/LISA-7B-v1'

# 或降低图像分辨率
--image_size=512
```

### 3. 推理速度慢

**问题**：推理时间过长

**解决方案**：
- 使用 `bf16` 精度（A100/H100）
- 安装 `flash-attn` 加速注意力计算
- 使用更小的模型（7B vs 13B）

### 4. 分割结果不准确

**问题**：分割 mask 不理想

**解决方案**：
- 使用更精确的提示词
- 尝试不同的提示词表达方式
- 使用解释版模型（`-explanatory`）查看模型理解

### 5. 提示词格式错误

**问题**：模型无法理解提示词

**解决方案**：
- 使用标准格式："Can you segment xxx in this image?"
- 添加 "Please output segmentation mask"
- 检查标点符号和语法

### 6. CLIP 模型下载失败

**问题**：无法下载 `openai/clip-vit-large-patch14`

**解决方案**：
```bash
# 手动下载 CLIP 模型
from transformers import CLIPModel
model = CLIPModel.from_pretrained("openai/clip-vit-large-patch14")
```

---

## 📝 完整推理示例

### 示例 1：基础分割

```bash
# 1. 启动推理
CUDA_VISIBLE_DEVICES=0 python chat.py \
  --version='xinlai/LISA-13B-llama2-v1' \
  --precision='fp16' \
  --load_in_4bit

# 2. 输入提示词
Please input your prompt: Can you segment the dog in this image?

# 3. 输入图片路径
Please input the image path: test_images/dog.jpg

# 4. 查看结果
# - vis_output/dog_mask_0.jpg（分割 mask）
# - vis_output/dog_masked_img_0.jpg（可视化结果）
```

### 示例 2：Web 界面

```bash
# 1. 启动 Web 服务
CUDA_VISIBLE_DEVICES=0 python app.py \
  --version='xinlai/LISA-13B-llama2-v1-explanatory' \
  --load_in_4bit

# 2. 打开浏览器访问 http://localhost:7860

# 3. 上传图片，输入提示词，点击提交
```

---

## 🎯 快速开始检查清单

### 推理准备

- [ ] 安装 Python 3.8-3.10
- [ ] 安装依赖：`pip install -r requirements.txt`
- [ ] 安装 flash-attn（可选）：`pip install flash-attn --no-build-isolation`
- [ ] 确保有足够的显存（至少 9GB for 13B 4-bit）
- [ ] 准备测试图片

### 模型下载

- [ ] 选择模型版本（13B 或 7B）
- [ ] 首次运行会自动下载，或手动下载到本地
- [ ] 确保网络连接正常（访问 HuggingFace）

### 运行推理

- [ ] 选择合适的精度和量化方式
- [ ] 运行 `chat.py` 或 `app.py`
- [ ] 输入提示词和图片路径
- [ ] 查看分割结果

---

## 📚 相关资源

- **论文**：https://arxiv.org/abs/2308.00692
- **GitHub**：https://github.com/dvlab-research/LISA
- **HuggingFace 模型**：https://huggingface.co/xinlai
- **在线 Demo**：http://103.170.5.190:7860/

---

## 💡 使用建议

1. **首次使用**：建议先用 7B 模型 + 4-bit 量化测试
2. **生产环境**：使用 13B 模型 + bf16 精度获得最佳效果
3. **提示词优化**：尝试不同的表达方式，找到最佳提示词
4. **批量处理**：可以修改代码实现批量图片处理

---

*最后更新：基于 LISA 仓库最新代码分析*

